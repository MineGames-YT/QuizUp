# дополнительные зависимости, если вы хотите собрать "десктопную" оболочку.
# обычный веб-запуск (python app.py) этого файла не требует.

# pywebview/pyinstaller пока не всегда дружат с самыми новыми версиями python,
# поэтому ставим их только если python < 3.13.
pywebview==4.4.1; python_version < '3.13'
pyinstaller>=6.10.0; python_version < '3.13'
